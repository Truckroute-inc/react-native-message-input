import type {
  NativeSubmission,
  NativeSubmissionOptions,
} from "./native-submission-types";

export function useNativeSubmission(
  _options: NativeSubmissionOptions,
): NativeSubmission | null {
  return null;
}
